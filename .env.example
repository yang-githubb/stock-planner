# Copy this file to backend/.env and fill in your values.
# The backend loads backend/.env when you run uvicorn from the backend/ folder.

# Finnhub API key (https://finnhub.io/)
FINNHUB_API_KEY=your_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4.1-mini

# Supabase Auth JWT verification — required: watchlists, portfolios, chat and
# insider ingestion all need a signed-in user, and the backend returns 503 on
# those endpoints until SUPABASE_JWKS_URL is set.
# Example:
# SUPABASE_JWKS_URL=https://[PROJECT_REF].supabase.co/auth/v1/.well-known/jwks.json
# SUPABASE_JWT_ISSUER=https://[PROJECT_REF].supabase.co/auth/v1
SUPABASE_JWKS_URL=
SUPABASE_JWT_ISSUER=

# Supabase PostgreSQL (recommended)
# Dashboard: Project Settings → Database → Connection string → URI
#
# Local dev (faster): Direct connection, port 5432
# DATABASE_URL=postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres
#
# Production / serverless (many short-lived workers): Transaction pooler, port 6543
# DATABASE_URL=postgresql+asyncpg://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
#
DATABASE_URL=postgresql+asyncpg://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres

# Optional: local SQLite for offline experiments only (not recommended for production)
# DATABASE_URL=sqlite+aiosqlite:///./stock_platform.db

# CORS origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Trending stocks on dashboard (comma-separated)
# TRENDING_SYMBOLS=AAPL,MSFT,GOOGL,AMZN,NVDA,TSLA

# LOCAL DEV ONLY — disables TLS certificate verification for the database
# connection. Never set this in production. Only use it if a local proxy
# breaks the certificate chain and you understand the risk:
# DATABASE_SSL_VERIFY=false

# Background jobs
# JOBS_ENABLED=true
# INSIDER_INGEST_INTERVAL_MINUTES=30

# Read-only demo account (see backend/scripts/seed_demo_account.py)
# DEMO_USER_EMAIL=demo@stockplanner.app
